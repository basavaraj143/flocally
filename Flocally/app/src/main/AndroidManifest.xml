<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.appsriv_02.flocally">


    <!-- Internet Permissions -->
    <uses-permission android:name="android.permission.INTERNET" />

    <android:uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />
    <android:uses-permission android:name="android.permission.READ_PHONE_STATE" />
    <android:uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" />

    <application
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:theme="@style/Theme.AppCompat.Light.NoActionBar"
        >
        <activity
            android:name=".MainTabActivity"
            android:configChanges="keyboardHidden|orientation"
            android:label="@string/app_name"
            >
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- Inbox Activity -->
        <activity android:name="com.example.appsriv_02.flocally.InboxActivity" />

        <!-- Outbox Activity -->
        <activity android:name="com.example.appsriv_02.flocally.OutboxActivity" />

        <!-- Profile Activity -->
        <activity android:name="com.example.appsriv_02.flocally.ProfileActivity" />
        <activity
            android:name="com.example.appsriv_02.flocally.Lunch_View"
            android:label="@string/title_activity_lunch__view" />
        <activity
            android:name="com.example.appsriv_02.flocally.SnacksView"
            android:label="@string/title_activity_snacks_view"></activity>
    </application>

</manifest>
